---
interface Props { type?: 'check'; size?: 'large' }
const { size } = Astro.props;
---
<span class:list={['status-icon', { large: size === 'large' }]} aria-hidden="true">
    <svg viewBox="0 0 24 24" fill="none"><path d="m7 12.5 3.1 3L17.5 8" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" /></svg>
</span>
